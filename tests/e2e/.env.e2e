# Environment variables shared across e2e scripts

# Host:port pairs used by the e2e scripts
ENVOY_HOST=envoy:8081
HTTPBIN_HOST=httpbin:8080
SSE_HOST=sse-server:8080

# Paths used by the e2e scripts
SSE_PATH=/events
HEALTH_PATH=/health

# Curl timing defaults for tests
CONNECT_TIMEOUT=5
MAX_TIME=5

# Optional: turn on script tracing
# DEBUG=true